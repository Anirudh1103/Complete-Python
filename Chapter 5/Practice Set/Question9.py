#Can you change the values inside a list which is contained in set S?
s = {8, 7, 12, "Harry", [1,2]}

# We cant include a list within a set but we can include a list within an dictionary.
# Sets dont have index as they are not ordered
#Therefore we cant change values here